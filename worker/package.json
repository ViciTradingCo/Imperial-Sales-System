{
  "name": "eec-sales-system-api",
  "version": "0.1.0",
  "description": "Backend API (Cloudflare Worker) for Vici Trading Co. — the trust boundary in front of the D1 datastore.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^3.78.0"
  }
}
