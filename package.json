{
  "name": "eec-sales-system",
  "version": "0.1.0",
  "description": "Vici Trading Co. sales network — static web frontend (GitHub Pages) backed by a Cloudflare Worker + D1 API.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "node scripts/audit.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
